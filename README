prentice - apprentice wizard roguelike or rather at this point a demo
of how an ECS could be written using sqlite. to build and run on
OpenBSD 6.5

  doas pkg_add tcl-8.5.19p4 sqlite3-tcl-3.24.0
  make
  ./prentice

other OS will require other commands and other amounts of work. notable
files include

 * game.db - copy of the database may get saved here to inpsect
   with sqlite3
 * init.tcl - where most everything is
 * log - where STDERR from the program is sent
 * main.c - bootstraps ncurses and TCL
 * main.tcl - main game loop
